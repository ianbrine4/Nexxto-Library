local RiftUI = {}

local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local HttpService = game:GetService("HttpService")
local TextService = game:GetService("TextService")

local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")

RiftUI.Theme = {
	Background = Color3.fromRGB(25, 25, 28),
	BackgroundSecondary = Color3.fromRGB(35, 35, 40),
	BackgroundTertiary = Color3.fromRGB(48, 48, 55),
	Text = Color3.fromRGB(245, 245, 245),
	TextSecondary = Color3.fromRGB(170, 170, 180),
	TextDark = Color3.fromRGB(100, 100, 110),
	Accent = Color3.fromRGB(255, 255, 255),
	Success = Color3.fromRGB(85, 210, 130),
	Error = Color3.fromRGB(220, 85, 90),
	Warning = Color3.fromRGB(255, 185, 85),
	Stroke = Color3.fromRGB(255, 255, 255),
	Inactive = Color3.fromRGB(110, 110, 120),
	Indicator = Color3.fromRGB(255, 255, 255),
	Hover = Color3.fromRGB(55, 55, 62),
	Glow = Color3.fromRGB(255, 255, 255)
}

RiftUI.Animation = {
	Fast = TweenInfo.new(0.12, Enum.EasingStyle.Quad, Enum.EasingDirection.Out),
	Normal = TweenInfo.new(0.22, Enum.EasingStyle.Quad, Enum.EasingDirection.Out),
	Smooth = TweenInfo.new(0.32, Enum.EasingStyle.Quad, Enum.EasingDirection.Out),
	Slow = TweenInfo.new(0.45, Enum.EasingStyle.Quad, Enum.EasingDirection.Out),
	Elastic = TweenInfo.new(0.38, Enum.EasingStyle.Back, Enum.EasingDirection.Out),
	Spring = TweenInfo.new(0.55, Enum.EasingStyle.Elastic, Enum.EasingDirection.Out)
}

local function Create(className, properties)
	local instance = Instance.new(className)
	for property, value in pairs(properties or {}) do
		instance[property] = value
	end
	return instance
end

local function Tween(instance, tweenInfo, properties)
	local tween = TweenService:Create(instance, tweenInfo, properties)
	tween:Play()
	return tween
end

local function SetStroke(instance, color, thickness)
	local stroke = Create("UIStroke", {
		Color = color or RiftUI.Theme.Stroke,
		Thickness = thickness or 1,
		ApplyStrokeMode = Enum.ApplyStrokeMode.Border,
		Parent = instance
	})
	return stroke
end

local function SetCorner(instance, radius)
	local corner = Create("UICorner", {
		CornerRadius = radius or UDim.new(0, 8),
		Parent = instance
	})
	return corner
end

local function SetPadding(instance, padding)
	local pad = Create("UIPadding", {
		PaddingLeft = UDim.new(0, padding or 12),
		PaddingRight = UDim.new(0, padding or 12),
		PaddingTop = UDim.new(0, padding or 12),
		PaddingBottom = UDim.new(0, padding or 12),
		Parent = instance
	})
	return pad
end

local function SetShadow(instance, transparency)
	local shadow = Create("ImageLabel", {
		Name = "Shadow",
		BackgroundTransparency = 1,
		Image = "rbxassetid://1316045217",
		ImageColor3 = Color3.fromRGB(0, 0, 0),
		ImageTransparency = transparency or 0.75,
		ScaleType = Enum.ScaleType.Slice,
		SliceCenter = Rect.new(10, 10, 118, 118),
		Size = UDim2.new(1, 24, 1, 24),
		Position = UDim2.new(0, -12, 0, -12),
		ZIndex = instance.ZIndex - 1,
		Parent = instance
	})
	return shadow
end

local function MakeDraggable(frame, dragArea)
	local dragging = false
	local dragInput, dragStart, startPos
	
	dragArea = dragArea or frame
	
	dragArea.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
			dragging = true
			dragStart = input.Position
			startPos = frame.Position
			
			input.Changed:Connect(function()
				if input.UserInputState == Enum.UserInputState.End then
					dragging = false
				end
			end)
		end
	end)
	
	dragArea.InputChanged:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
			dragInput = input
		end
	end)
	
	UserInputService.InputChanged:Connect(function(input)
		if input == dragInput and dragging then
			local delta = input.Position - dragStart
			frame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
		end
	end)
end

function RiftUI:CreateWindow(config)
	config = config or {}
	local title = config.title or "Rift UI Library"
	local subtitle = config.subtitle or "Clean modern Roblox UI"
	local iconText = config.text or "Rift"
	local size = config.size or Vector2.new(600, 400)
	local accentColor = config.color or RiftUI.Theme.Accent
	local transparent = config.transparent or false
	
	local screenGui = Create("ScreenGui", {
		Name = "RiftUI_" .. HttpService:GenerateGUID(false),
		Parent = PlayerGui,
		ResetOnSpawn = false,
		ZIndexBehavior = Enum.ZIndexBehavior.Sibling
	})
	
	local mainFrame = Create("Frame", {
		Name = "MainFrame",
		Size = UDim2.new(0, size.X, 0, size.Y),
		Position = UDim2.new(0.5, -size.X/2, 0.5, -size.Y/2),
		BackgroundColor3 = RiftUI.Theme.Background,
		BackgroundTransparency = transparent and 0.15 or 0,
		BorderSizePixel = 0,
		ClipsDescendants = true,
		Parent = screenGui
	})
	SetCorner(mainFrame, UDim.new(0, 12))
	SetStroke(mainFrame, RiftUI.Theme.Stroke, 1)
	SetShadow(mainFrame, transparent and 0.85 or 0.7)
	
	local header = Create("Frame", {
		Name = "Header",
		Size = UDim2.new(1, 0, 0, 72),
		BackgroundColor3 = RiftUI.Theme.BackgroundSecondary,
		BackgroundTransparency = transparent and 0.1 or 0,
		BorderSizePixel = 0,
		Parent = mainFrame
	})
	
	local headerStroke = Create("Frame", {
		Name = "HeaderStroke",
		Size = UDim2.new(1, 0, 0, 1),
		Position = UDim2.new(0, 0, 1, 0),
		BackgroundColor3 = RiftUI.Theme.BackgroundTertiary,
		BackgroundTransparency = 0.5,
		BorderSizePixel = 0,
		Parent = header
	})
	
	local titleLabel = Create("TextLabel", {
		Name = "Title",
		Text = title,
		Font = Enum.Font.GothamBold,
		TextSize = 18,
		TextColor3 = RiftUI.Theme.Text,
		BackgroundTransparency = 1,
		Size = UDim2.new(0, 250, 0, 22),
		Position = UDim2.new(0, 18, 0, 14),
		TextXAlignment = Enum.TextXAlignment.Left,
		Parent = header
	})
	
	local subtitleLabel = Create("TextLabel", {
		Name = "Subtitle",
		Text = subtitle,
		Font = Enum.Font.Gotham,
		TextSize = 12,
		TextColor3 = RiftUI.Theme.TextSecondary,
		BackgroundTransparency = 1,
		Size = UDim2.new(0, 250, 0, 16),
		Position = UDim2.new(0, 18, 0, 36),
		TextXAlignment = Enum.TextXAlignment.Left,
		Parent = header
	})
	
	local pingFrame = Create("Frame", {
		Name = "PingFrame",
		Size = UDim2.new(0, 70, 0, 24),
		Position = UDim2.new(1, -200, 0, 24),
		BackgroundColor3 = RiftUI.Theme.BackgroundTertiary,
		BackgroundTransparency = transparent and 0.3 or 0,
		BorderSizePixel = 0,
		Parent = header
	})
	SetCorner(pingFrame, UDim.new(0, 6))
	
	local pingDot = Create("Frame", {
		Name = "PingDot",
		Size = UDim2.new(0, 6, 0, 6),
		Position = UDim2.new(0, 10, 0.5, -3),
		BackgroundColor3 = RiftUI.Theme.Success,
		BorderSizePixel = 0,
		Parent = pingFrame
	})
	SetCorner(pingDot, UDim.new(1, 0))
	
	local pingLabel = Create("TextLabel", {
		Name = "Ping",
		Text = "0 ms",
		Font = Enum.Font.Gotham,
		TextSize = 11,
		TextColor3 = RiftUI.Theme.TextSecondary,
		BackgroundTransparency = 1,
		Size = UDim2.new(0, 45, 0, 20),
		Position = UDim2.new(0, 22, 0, 2),
		TextXAlignment = Enum.TextXAlignment.Left,
		Parent = pingFrame
	})
	
	local pingConnection = RunService.Heartbeat:Connect(function()
		local ping = math.floor(LocalPlayer:GetNetworkPing() * 1000)
		pingLabel.Text = ping .. " ms"
		if ping < 50 then
			pingDot.BackgroundColor3 = RiftUI.Theme.Success
			pingLabel.TextColor3 = RiftUI.Theme.Success
		elseif ping < 100 then
			pingDot.BackgroundColor3 = RiftUI.Theme.Warning
			pingLabel.TextColor3 = RiftUI.Theme.Warning
		else
			pingDot.BackgroundColor3 = RiftUI.Theme.Error
			pingLabel.TextColor3 = RiftUI.Theme.Error
		end
	end)
	
	local hideButton = Create("TextButton", {
		Name = "HideButton",
		Text = "-",
		Font = Enum.Font.GothamBold,
		TextSize = 18,
		TextColor3 = RiftUI.Theme.TextSecondary,
		BackgroundColor3 = RiftUI.Theme.BackgroundTertiary,
		Size = UDim2.new(0, 30, 0, 30),
		Position = UDim2.new(1, -78, 0, 21),
		Parent = header
	})
	SetCorner(hideButton, UDim.new(0, 8))
	
	hideButton.MouseEnter:Connect(function()
		Tween(hideButton, RiftUI.Animation.Fast, {BackgroundColor3 = RiftUI.Theme.Accent, TextColor3 = RiftUI.Theme.Background})
	end)
	
	hideButton.MouseLeave:Connect(function()
		Tween(hideButton, RiftUI.Animation.Fast, {BackgroundColor3 = RiftUI.Theme.BackgroundTertiary, TextColor3 = RiftUI.Theme.TextSecondary})
	end)
	
	local closeButton = Create("TextButton", {
		Name = "CloseButton",
		Text = "X",
		Font = Enum.Font.GothamBold,
		TextSize = 13,
		TextColor3 = RiftUI.Theme.TextSecondary,
		BackgroundColor3 = RiftUI.Theme.BackgroundTertiary,
		Size = UDim2.new(0, 30, 0, 30),
		Position = UDim2.new(1, -40, 0, 21),
		Parent = header
	})
	SetCorner(closeButton, UDim.new(0, 8))
	
	closeButton.MouseEnter:Connect(function()
		Tween(closeButton, RiftUI.Animation.Fast, {BackgroundColor3 = RiftUI.Theme.Error, TextColor3 = RiftUI.Theme.Text})
	end)
	
	closeButton.MouseLeave:Connect(function()
		Tween(closeButton, RiftUI.Animation.Fast, {BackgroundColor3 = RiftUI.Theme.BackgroundTertiary, TextColor3 = RiftUI.Theme.TextSecondary})
	end)
	
	local modalOverlay = Create("Frame", {
		Name = "ModalOverlay",
		Size = UDim2.new(1, 0, 1, 0),
		BackgroundColor3 = Color3.fromRGB(0, 0, 0),
		BackgroundTransparency = 1,
		Visible = false,
		ZIndex = 100,
		Parent = mainFrame
	})
	
	local modalFrame = Create("Frame", {
		Name = "ModalFrame",
		Size = UDim2.new(0, 300, 0, 150),
		Position = UDim2.new(0.5, -150, 0.5, -75),
		BackgroundColor3 = RiftUI.Theme.BackgroundSecondary,
		BorderSizePixel = 0,
		BackgroundTransparency = 1,
		ZIndex = 101,
		Parent = modalOverlay
	})
	SetCorner(modalFrame, UDim.new(0, 12))
	SetStroke(modalFrame, RiftUI.Theme.Stroke, 1)
	
	local modalTitle = Create("TextLabel", {
		Name = "ModalTitle",
		Text = "Confirm Close",
		Font = Enum.Font.GothamBold,
		TextSize = 17,
		TextColor3 = RiftUI.Theme.Text,
		BackgroundTransparency = 1,
		Size = UDim2.new(1, 0, 0, 26),
		Position = UDim2.new(0, 0, 0, 24),
		ZIndex = 102,
		Parent = modalFrame
	})
	
	local modalText = Create("TextLabel", {
		Name = "ModalText",
		Text = "Are you sure you want to close?",
		Font = Enum.Font.Gotham,
		TextSize = 13,
		TextColor3 = RiftUI.Theme.TextSecondary,
		BackgroundTransparency = 1,
		Size = UDim2.new(1, 0, 0, 22),
		Position = UDim2.new(0, 0, 0, 54),
		ZIndex = 102,
		Parent = modalFrame
	})
	
	local confirmButton = Create("TextButton", {
		Name = "ConfirmButton",
		Text = "Confirm",
		Font = Enum.Font.GothamBold,
		TextSize = 13,
		TextColor3 = RiftUI.Theme.Background,
		BackgroundColor3 = RiftUI.Theme.Success,
		Size = UDim2.new(0, 110, 0, 36),
		Position = UDim2.new(0, 30, 1, -56),
		ZIndex = 102,
		Parent = modalFrame
	})
	SetCorner(confirmButton, UDim.new(0, 8))
	
	local declineButton = Create("TextButton", {
		Name = "DeclineButton",
		Text = "Decline",
		Font = Enum.Font.GothamBold,
		TextSize = 13,
		TextColor3 = RiftUI.Theme.Text,
		BackgroundColor3 = RiftUI.Theme.BackgroundTertiary,
		Size = UDim2.new(0, 110, 0, 36),
		Position = UDim2.new(1, -140, 1, -56),
		ZIndex = 102,
		Parent = modalFrame
	})
	SetCorner(declineButton, UDim.new(0, 8))
	
	confirmButton.MouseEnter:Connect(function()
		Tween(confirmButton, RiftUI.Animation.Fast, {BackgroundColor3 = Color3.fromRGB(105, 230, 150)})
	end)
	
	confirmButton.MouseLeave:Connect(function()
		Tween(confirmButton, RiftUI.Animation.Fast, {BackgroundColor3 = RiftUI.Theme.Success})
	end)
	
	declineButton.MouseEnter:Connect(function()
		Tween(declineButton, RiftUI.Animation.Fast, {BackgroundColor3 = RiftUI.Theme.Hover})
	end)
	
	declineButton.MouseLeave:Connect(function()
		Tween(declineButton, RiftUI.Animation.Fast, {BackgroundColor3 = RiftUI.Theme.BackgroundTertiary})
	end)
	
	local contentFrame = Create("Frame", {
		Name = "Content",
		Size = UDim2.new(1, 0, 1, -72),
		Position = UDim2.new(0, 0, 0, 72),
		BackgroundTransparency = 1,
		Parent = mainFrame
	})
	
	local tabContainer = Create("Frame", {
		Name = "TabContainer",
		Size = UDim2.new(0, 150, 1, 0),
		BackgroundColor3 = RiftUI.Theme.Background,
		BackgroundTransparency = transparent and 0.2 or 0,
		BorderSizePixel = 0,
		Parent = contentFrame
	})
	
	local tabSeparator = Create("Frame", {
		Name = "TabSeparator",
		Size = UDim2.new(0, 1, 1, -24),
		Position = UDim2.new(1, 0, 0, 12),
		BackgroundColor3 = RiftUI.Theme.BackgroundTertiary,
		BackgroundTransparency = 0.6,
		BorderSizePixel = 0,
		Parent = tabContainer
	})
	
	local tabScroll = Create("ScrollingFrame", {
		Name = "TabScroll",
		Size = UDim2.new(1, -1, 1, 0),
		BackgroundTransparency = 1,
		ScrollBarThickness = 0,
		CanvasSize = UDim2.new(0, 0, 0, 0),
		Parent = tabContainer
	})
	
	local tabList = Create("UIListLayout", {
		Padding = UDim.new(0, 4),
		Parent = tabScroll
	})
	
	local tabPadding = Create("UIPadding", {
		PaddingTop = UDim.new(0, 14),
		PaddingLeft = UDim.new(0, 12),
		PaddingRight = UDim.new(0, 12),
		Parent = tabScroll
	})
	
	local indicator = Create("Frame", {
		Name = "Indicator",
		Size = UDim2.new(0, 3, 0, 26),
		Position = UDim2.new(0, 0, 0, 14),
		BackgroundColor3 = RiftUI.Theme.Indicator,
		BorderSizePixel = 0,
		ZIndex = 10,
		Parent = tabScroll
	})
	SetCorner(indicator, UDim.new(0, 2))
	
	local pageContainer = Create("Frame", {
		Name = "PageContainer",
		Size = UDim2.new(1, -150, 1, 0),
		Position = UDim2.new(0, 150, 0, 0),
		BackgroundTransparency = 1,
		Parent = contentFrame
	})
	
	local profileFrame = Create("Frame", {
		Name = "Profile",
		Size = UDim2.new(0, size.X, 0, 64),
		Position = UDim2.new(0.5, -size.X/2, 0.5, size.Y/2 + 12),
		BackgroundColor3 = RiftUI.Theme.Background,
		BackgroundTransparency = transparent and 0.15 or 0,
		BorderSizePixel = 0,
		Parent = screenGui
	})
	SetCorner(profileFrame, UDim.new(0, 12))
	SetStroke(profileFrame, RiftUI.Theme.Stroke, 1)
	SetShadow(profileFrame, transparent and 0.85 or 0.7)
	
	local avatarImage = Create("ImageLabel", {
		Name = "Avatar",
		Size = UDim2.new(0, 46, 0, 46),
		Position = UDim2.new(0, 12, 0, 9),
		BackgroundColor3 = RiftUI.Theme.BackgroundSecondary,
		Image = "",
		Parent = profileFrame
	})
	SetCorner(avatarImage, UDim.new(0, 10))
	
	local usernameLabel = Create("TextLabel", {
		Name = "Username",
		Text = LocalPlayer.DisplayName,
		Font = Enum.Font.GothamBold,
		TextSize = 14,
		TextColor3 = RiftUI.Theme.Text,
		BackgroundTransparency = 1,
		Size = UDim2.new(0, 200, 0, 20),
		Position = UDim2.new(0, 68, 0, 12),
		TextXAlignment = Enum.TextXAlignment.Left,
		Parent = profileFrame
	})
	
	local displayLabel = Create("TextLabel", {
		Name = "DisplayName",
		Text = "@" .. LocalPlayer.Name,
		Font = Enum.Font.Gotham,
		TextSize = 12,
		TextColor3 = RiftUI.Theme.TextSecondary,
		BackgroundTransparency = 1,
		Size = UDim2.new(0, 200, 0, 16),
		Position = UDim2.new(0, 68, 0, 34),
		TextXAlignment = Enum.TextXAlignment.Left,
		Parent = profileFrame
	})
	
	local success, result = pcall(function()
		local userId = LocalPlayer.UserId
		local thumbType = Enum.ThumbnailType.HeadShot
		local thumbSize = Enum.ThumbnailSize.Size420x420
		local content, isReady = Players:GetUserThumbnailAsync(userId, thumbType, thumbSize)
		return content
	end)
	
	if success then
		avatarImage.Image = result
	end
	
	local draggableIcon = Create("TextButton", {
		Name = "DraggableIcon",
		Size = UDim2.new(0, 64, 0, 64),
		Position = UDim2.new(0, 50, 0.5, -32),
		BackgroundColor3 = RiftUI.Theme.Background,
		BackgroundTransparency = transparent and 0.15 or 0,
		Text = iconText,
		Font = Enum.Font.GothamBold,
		TextSize = 12,
		TextColor3 = RiftUI.Theme.Text,
		Visible = false,
		Parent = screenGui
	})
	SetCorner(draggableIcon, UDim.new(1, 0))
	SetStroke(draggableIcon, RiftUI.Theme.Stroke, 1)
	SetShadow(draggableIcon, 0.6)
	
	MakeDraggable(draggableIcon)
	
	draggableIcon.MouseEnter:Connect(function()
		Tween(draggableIcon, RiftUI.Animation.Fast, {Size = UDim2.new(0, 70, 0, 70)})
	end)
	
	draggableIcon.MouseLeave:Connect(function()
		Tween(draggableIcon, RiftUI.Animation.Fast, {Size = UDim2.new(0, 64, 0, 64)})
	end)
	
	local windowOpen = true
	local iconPosition = draggableIcon.Position
	
	local function OpenModal()
		modalOverlay.Visible = true
		Tween(modalOverlay, RiftUI.Animation.Normal, {BackgroundTransparency = 0.6})
		Tween(modalFrame, RiftUI.Animation.Elastic, {BackgroundTransparency = 0})
		modalTitle.TextTransparency = 1
		modalText.TextTransparency = 1
		confirmButton.BackgroundTransparency = 1
		confirmButton.TextTransparency = 1
		declineButton.BackgroundTransparency = 1
		declineButton.TextTransparency = 1
		
		task.delay(0.1, function()
			Tween(modalTitle, RiftUI.Animation.Fast, {TextTransparency = 0})
			Tween(modalText, RiftUI.Animation.Fast, {TextTransparency = 0})
			Tween(confirmButton, RiftUI.Animation.Fast, {BackgroundTransparency = 0, TextTransparency = 0})
			Tween(declineButton, RiftUI.Animation.Fast, {BackgroundTransparency = 0, TextTransparency = 0})
		end)
	end
	
	local function CloseModal()
		Tween(modalOverlay, RiftUI.Animation.Normal, {BackgroundTransparency = 1})
		Tween(modalFrame, RiftUI.Animation.Normal, {BackgroundTransparency = 1})
		Tween(modalTitle, RiftUI.Animation.Fast, {TextTransparency = 1})
		Tween(modalText, RiftUI.Animation.Fast, {TextTransparency = 1})
		Tween(confirmButton, RiftUI.Animation.Fast, {BackgroundTransparency = 1, TextTransparency = 1})
		Tween(declineButton, RiftUI.Animation.Fast, {BackgroundTransparency = 1, TextTransparency = 1})
		
		task.delay(0.3, function()
			modalOverlay.Visible = false
		end)
	end
	
	local function HideWindow()
		windowOpen = false
		iconPosition = draggableIcon.Position
		Tween(mainFrame, RiftUI.Animation.Smooth, {Size = UDim2.new(0, 0, 0, 0), Position = UDim2.new(0.5, 0, 0.5, 0)})
		Tween(profileFrame, RiftUI.Animation.Smooth, {Size = UDim2.new(0, 0, 0, 0), Position = UDim2.new(0.5, 0, 0.5, 0)})
		
		task.delay(0.3, function()
			mainFrame.Visible = false
			profileFrame.Visible = false
			draggableIcon.Visible = true
			draggableIcon.Position = iconPosition
			Tween(draggableIcon, RiftUI.Animation.Elastic, {Size = UDim2.new(0, 64, 0, 64)})
		end)
	end
	
	local function ShowWindow()
		windowOpen = true
		iconPosition = draggableIcon.Position
		Tween(draggableIcon, RiftUI.Animation.Fast, {Size = UDim2.new(0, 0, 0, 0)})
		
		task.delay(0.15, function()
			draggableIcon.Visible = false
			mainFrame.Visible = true
			profileFrame.Visible = true
			mainFrame.Size = UDim2.new(0, 0, 0, 0)
			mainFrame.Position = UDim2.new(0.5, 0, 0.5, 0)
			Tween(mainFrame, RiftUI.Animation.Elastic, {Size = UDim2.new(0, size.X, 0, size.Y), Position = UDim2.new(0.5, -size.X/2, 0.5, -size.Y/2)})
			Tween(profileFrame, RiftUI.Animation.Elastic, {Size = UDim2.new(0, size.X, 0, 64), Position = UDim2.new(0.5, -size.X/2, 0.5, size.Y/2 + 12)})
		end)
	end
	
	closeButton.MouseButton1Click:Connect(OpenModal)
	
	confirmButton.MouseButton1Click:Connect(function()
		pingConnection:Disconnect()
		screenGui:Destroy()
	end)
	
	declineButton.MouseButton1Click:Connect(CloseModal)
	
	hideButton.MouseButton1Click:Connect(HideWindow)
	
	draggableIcon.MouseButton1Click:Connect(ShowWindow)
	
	MakeDraggable(mainFrame, header)
	MakeDraggable(profileFrame)
	
	local window = {
		ScreenGui = screenGui,
		MainFrame = mainFrame,
		ContentFrame = contentFrame,
		TabContainer = tabContainer,
		TabScroll = tabScroll,
		PageContainer = pageContainer,
		Indicator = indicator,
		Tabs = {},
		CurrentTab = nil
	}
	
	function window:AddTab(tabName)
		local tabButton = Create("TextButton", {
			Name = tabName .. "Tab",
			Text = tabName,
			Font = Enum.Font.Gotham,
			TextSize = 13,
			TextColor3 = RiftUI.Theme.TextSecondary,
			BackgroundColor3 = RiftUI.Theme.Background,
			BackgroundTransparency = 1,
			Size = UDim2.new(1, 0, 0, 34),
			Parent = self.TabScroll
		})
		SetCorner(tabButton, UDim.new(0, 6))
		
		local tabPage = Create("ScrollingFrame", {
			Name = tabName .. "Page",
			Size = UDim2.new(1, 0, 1, 0),
			BackgroundTransparency = 1,
			ScrollBarThickness = 3,
			ScrollBarImageColor3 = RiftUI.Theme.BackgroundTertiary,
			CanvasSize = UDim2.new(0, 0, 0, 0),
			Visible = false,
			Parent = self.PageContainer
		})
		SetPadding(tabPage, 18)
		
		local pageList = Create("UIListLayout", {
			Padding = UDim.new(0, 10),
			Parent = tabPage
		})
		
		pageList:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
			tabPage.CanvasSize = UDim2.new(0, 0, 0, pageList.AbsoluteContentSize.Y + 36)
		end)
		
		local tab = {
			Button = tabButton,
			Page = tabPage,
			Name = tabName,
			Components = {}
		}
		
		tabButton.MouseEnter:Connect(function()
			if self.CurrentTab ~= tab then
				Tween(tabButton, RiftUI.Animation.Fast, {TextColor3 = RiftUI.Theme.Text, BackgroundColor3 = RiftUI.Theme.BackgroundSecondary})
			end
		end)
		
		tabButton.MouseLeave:Connect(function()
			if self.CurrentTab ~= tab then
				Tween(tabButton, RiftUI.Animation.Fast, {TextColor3 = RiftUI.Theme.TextSecondary, BackgroundColor3 = RiftUI.Theme.Background})
			end
		end)
		
		tabButton.MouseButton1Click:Connect(function()
			self:SelectTab(tab)
		end)
		
		function tab:AddSeparator()
			local separator = Create("Frame", {
				Name = "Separator",
				Size = UDim2.new(1, 0, 0, 1),
				BackgroundColor3 = RiftUI.Theme.BackgroundTertiary,
				BackgroundTransparency = 0.5,
				BorderSizePixel = 0,
				Parent = self.Page
			})
			return separator
		end
		
		function tab:AddLabel(text)
			local label = Create("TextLabel", {
				Name = "Label",
				Text = text,
				Font = Enum.Font.GothamSemibold,
				TextSize = 13,
				TextColor3 = RiftUI.Theme.Text,
				BackgroundTransparency = 1,
				Size = UDim2.new(1, 0, 0, 22),
				TextXAlignment = Enum.TextXAlignment.Left,
				Parent = self.Page
			})
			return label
		end
		
		function tab:AddParagraph(text)
			local paragraph = Create("TextLabel", {
				Name = "Paragraph",
				Text = text,
				Font = Enum.Font.Gotham,
				TextSize = 12,
				TextColor3 = RiftUI.Theme.TextSecondary,
				BackgroundTransparency = 1,
				Size = UDim2.new(1, 0, 0, 0),
				AutomaticSize = Enum.AutomaticSize.Y,
				TextWrapped = true,
				TextXAlignment = Enum.TextXAlignment.Left,
				Parent = self.Page
			})
			return paragraph
		end
		
		function tab:AddButton(config)
			config = config or {}
			local text = config.text or "Button"
			local callback = config.callback or function() end
			
			local buttonFrame = Create("Frame", {
				Name = "Button",
				Size = UDim2.new(1, 0, 0, 40),
				BackgroundTransparency = 1,
				Parent = self.Page
			})
			
			local button = Create("TextButton", {
				Name = "ButtonElement",
				Text = "",
				BackgroundColor3 = RiftUI.Theme.BackgroundSecondary,
				Size = UDim2.new(1, 0, 1, 0),
				Parent = buttonFrame
			})
			SetCorner(button, UDim.new(0, 8))
			
			local cursorImage = Create("ImageLabel", {
				Name = "Cursor",
				Size = UDim2.new(0, 16, 0, 16),
				Position = UDim2.new(0, 14, 0.5, -8),
				BackgroundTransparency = 1,
				Image = "rbxassetid://3944720528",
				ImageColor3 = RiftUI.Theme.Text,
				Parent = button
			})
			
			local buttonText = Create("TextLabel", {
				Name = "Text",
				Text = text,
				Font = Enum.Font.GothamSemibold,
				TextSize = 13,
				TextColor3 = RiftUI.Theme.Text,
				BackgroundTransparency = 1,
				Size = UDim2.new(1, -50, 1, 0),
				Position = UDim2.new(0, 38, 0, 0),
				TextXAlignment = Enum.TextXAlignment.Left,
				Parent = button
			})
			
			local glow = Create("Frame", {
				Name = "Glow",
				Size = UDim2.new(1, 6, 1, 6),
				Position = UDim2.new(0, -3, 0, -3),
				BackgroundColor3 = accentColor,
				BackgroundTransparency = 1,
				BorderSizePixel = 0,
				ZIndex = button.ZIndex - 1,
				Parent = button
			})
			SetCorner(glow, UDim.new(0, 10))
			
			local clickFeedback = Create("Frame", {
				Name = "ClickFeedback",
				Size = UDim2.new(0, 0, 0, 0),
				Position = UDim2.new(0.5, 0, 0.5, 0),
				BackgroundColor3 = accentColor,
				BackgroundTransparency = 0.8,
				BorderSizePixel = 0,
				ZIndex = button.ZIndex + 1,
				Visible = false,
				Parent = button
			})
			SetCorner(clickFeedback, UDim.new(1, 0))
			
			button.MouseEnter:Connect(function()
				Tween(button, RiftUI.Animation.Fast, {BackgroundColor3 = RiftUI.Theme.Hover})
				Tween(cursorImage, RiftUI.Animation.Fast, {ImageColor3 = accentColor})
				Tween(glow, RiftUI.Animation.Fast, {BackgroundTransparency = 0.85})
				Tween(buttonText, RiftUI.Animation.Fast, {Position = UDim2.new(0, 42, 0, 0)})
				Tween(cursorImage, RiftUI.Animation.Fast, {Position = UDim2.new(0, 18, 0.5, -8)})
			end)
			
			button.MouseLeave:Connect(function()
				Tween(button, RiftUI.Animation.Fast, {BackgroundColor3 = RiftUI.Theme.BackgroundSecondary})
				Tween(cursorImage, RiftUI.Animation.Fast, {ImageColor3 = RiftUI.Theme.Text})
				Tween(glow, RiftUI.Animation.Fast, {BackgroundTransparency = 1})
				Tween(buttonText, RiftUI.Animation.Fast, {Position = UDim2.new(0, 38, 0, 0)})
				Tween(cursorImage, RiftUI.Animation.Fast, {Position = UDim2.new(0, 14, 0.5, -8)})
			end)
			
			button.MouseButton1Down:Connect(function()
				clickFeedback.Visible = true
				clickFeedback.Size = UDim2.new(0, 0, 0, 0)
				clickFeedback.BackgroundTransparency = 0.6
				Tween(clickFeedback, RiftUI.Animation.Fast, {Size = UDim2.new(1.2, 0, 1.5, 0), Position = UDim2.new(-0.1, 0, -0.25, 0), BackgroundTransparency = 1})
				Tween(button, RiftUI.Animation.Fast, {Size = UDim2.new(0.97, 0, 0.92, 0), Position = UDim2.new(0.015, 0, 0.04, 0)})
			end)
			
			button.MouseButton1Up:Connect(function()
				Tween(button, RiftUI.Animation.Elastic, {Size = UDim2.new(1, 0, 1, 0), Position = UDim2.new(0, 0, 0, 0)})
				task.delay(0.2, function()
					clickFeedback.Visible = false
				end)
			end)
			
			button.MouseButton1Click:Connect(callback)
			
			return button
		end
		
		function tab:AddToggle(config)
			config = config or {}
			local text = config.text or "Toggle"
			local default = config.default or false
			local callback = config.callback or function() end
			
			local toggleFrame = Create("Frame", {
				Name = "Toggle",
				Size = UDim2.new(1, 0, 0, 40),
				BackgroundTransparency = 1,
				Parent = self.Page
			})
			
			local label = Create("TextLabel", {
				Name = "Label",
				Text = text,
				Font = Enum.Font.Gotham,
				TextSize = 13,
				TextColor3 = RiftUI.Theme.Text,
				BackgroundTransparency = 1,
				Size = UDim2.new(1, -70, 1, 0),
				TextXAlignment = Enum.TextXAlignment.Left,
				Parent = toggleFrame
			})
			
			local switchBg = Create("Frame", {
				Name = "SwitchBg",
				Size = UDim2.new(0, 48, 0, 26),
				Position = UDim2.new(1, -48, 0.5, -13),
				BackgroundColor3 = default and RiftUI.Theme.Success or RiftUI.Theme.BackgroundTertiary,
				BorderSizePixel = 0,
				Parent = toggleFrame
			})
			SetCorner(switchBg, UDim.new(1, 0))
			
			local switchHandle = Create("Frame", {
				Name = "Handle",
				Size = UDim2.new(0, 20, 0, 20),
				Position = default and UDim2.new(1, -24, 0.5, -10) or UDim2.new(0, 4, 0.5, -10),
				BackgroundColor3 = RiftUI.Theme.Text,
				BorderSizePixel = 0,
				Parent = switchBg
			})
			SetCorner(switchHandle, UDim.new(1, 0))
			
			local enabled = default
			
			local function UpdateToggle()
				enabled = not enabled
				Tween(switchBg, RiftUI.Animation.Smooth, {BackgroundColor3 = enabled and RiftUI.Theme.Success or RiftUI.Theme.BackgroundTertiary})
				Tween(switchHandle, RiftUI.Animation.Smooth, {Position = enabled and UDim2.new(1, -24, 0.5, -10) or UDim2.new(0, 4, 0.5, -10)})
				callback(enabled)
			end
			
			switchBg.InputBegan:Connect(function(input)
				if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
					UpdateToggle()
				end
			end)
			
			return {
				Set = function(value)
					enabled = value
					Tween(switchBg, RiftUI.Animation.Smooth, {BackgroundColor3 = enabled and RiftUI.Theme.Success or RiftUI.Theme.BackgroundTertiary})
					Tween(switchHandle, RiftUI.Animation.Smooth, {Position = enabled and UDim2.new(1, -24, 0.5, -10) or UDim2.new(0, 4, 0.5, -10)})
					callback(enabled)
				end,
				Get = function()
					return enabled
				end
			}
		end
		
		function tab:AddSlider(config)
			config = config or {}
			local text = config.text or "Slider"
			local min = config.min or 0
			local max = config.max or 100
			local default = config.default or min
			local callback = config.callback or function() end
			
			local sliderFrame = Create("Frame", {
				Name = "Slider",
				Size = UDim2.new(1, 0, 0, 56),
				BackgroundTransparency = 1,
				Parent = self.Page
			})
			
			local label = Create("TextLabel", {
				Name = "Label",
				Text = text,
				Font = Enum.Font.Gotham,
				TextSize = 13,
				TextColor3 = RiftUI.Theme.Text,
				BackgroundTransparency = 1,
				Size = UDim2.new(0.6, 0, 0, 22),
				TextXAlignment = Enum.TextXAlignment.Left,
				Parent = sliderFrame
			})
			
			local valueFrame = Create("Frame", {
				Name = "ValueFrame",
				Size = UDim2.new(0, 50, 0, 22),
				Position = UDim2.new(1, -50, 0, 0),
				BackgroundColor3 = RiftUI.Theme.BackgroundTertiary,
				BorderSizePixel = 0,
				Parent = sliderFrame
			})
			SetCorner(valueFrame, UDim.new(0, 6))
			
			local valueLabel = Create("TextLabel", {
				Name = "Value",
				Text = tostring(default),
				Font = Enum.Font.GothamSemibold,
				TextSize = 12,
				TextColor3 = RiftUI.Theme.TextSecondary,
				BackgroundTransparency = 1,
				Size = UDim2.new(1, 0, 1, 0),
				Parent = valueFrame
			})
			
			local track = Create("Frame", {
				Name = "Track",
				Size = UDim2.new(1, 0, 0, 6),
				Position = UDim2.new(0, 0, 0, 40),
				BackgroundColor3 = RiftUI.Theme.BackgroundTertiary,
				BorderSizePixel = 0,
				Parent = sliderFrame
			})
			SetCorner(track, UDim.new(1, 0))
			
			local fill = Create("Frame", {
				Name = "Fill",
				Size = UDim2.new((default - min) / (max - min), 0, 1, 0),
				BackgroundColor3 = accentColor,
				BorderSizePixel = 0,
				Parent = track
			})
			SetCorner(fill, UDim.new(1, 0))
			
			local handle = Create("Frame", {
				Name = "Handle",
				Size = UDim2.new(0, 16, 0, 16),
				Position = UDim2.new((default - min) / (max - min), -8, 0.5, -8),
				BackgroundColor3 = RiftUI.Theme.Text,
				BorderSizePixel = 0,
				Parent = track
			})
			SetCorner(handle, UDim.new(1, 0))
			
			local handleGlow = Create("Frame", {
				Name = "HandleGlow",
				Size = UDim2.new(1, 8, 1, 8),
				Position = UDim2.new(0, -4, 0, -4),
				BackgroundColor3 = accentColor,
				BackgroundTransparency = 1,
				BorderSizePixel = 0,
				ZIndex = handle.ZIndex - 1,
				Parent = handle
			})
			SetCorner(handleGlow, UDim.new(1, 0))
			
			local dragging = false
			
			local function UpdateSlider(input)
				local pos = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
				local value = math.floor(min + (max - min) * pos)
				valueLabel.Text = tostring(value)
				Tween(fill, RiftUI.Animation.Fast, {Size = UDim2.new(pos, 0, 1, 0)})
				Tween(handle, RiftUI.Animation.Fast, {Position = UDim2.new(pos, -8, 0.5, -8)})
				callback(value)
			end
			
			track.InputBegan:Connect(function(input)
				if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
					dragging = true
					Tween(handleGlow, RiftUI.Animation.Fast, {BackgroundTransparency = 0.7})
					UpdateSlider(input)
				end
			end)
			
			track.InputChanged:Connect(function(input)
				if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
					UpdateSlider(input)
				end
			end)
			
			UserInputService.InputEnded:Connect(function(input)
				if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
					dragging = false
					Tween(handleGlow, RiftUI.Animation.Fast, {BackgroundTransparency = 1})
				end
			end)
			
			return {
				Set = function(value)
					local clamped = math.clamp(value, min, max)
					local pos = (clamped - min) / (max - min)
					valueLabel.Text = tostring(clamped)
					Tween(fill, RiftUI.Animation.Smooth, {Size = UDim2.new(pos, 0, 1, 0)})
					Tween(handle, RiftUI.Animation.Smooth, {Position = UDim2.new(pos, -8, 0.5, -8)})
					callback(clamped)
				end,
				Get = function()
					return tonumber(valueLabel.Text)
				end
			}
		end
		
		function tab:AddDropdown(config)
			config = config or {}
			local text = config.text or "Dropdown"
			local options = config.options or {}
			local default = config.default
			local callback = config.callback or function() end
			
			local dropdownFrame = Create("Frame", {
				Name = "Dropdown",
				Size = UDim2.new(1, 0, 0, 38),
				BackgroundTransparency = 1,
				ClipsDescendants = true,
				Parent = self.Page
			})
			
			local label = Create("TextLabel", {
				Name = "Label",
				Text = text,
				Font = Enum.Font.Gotham,
				TextSize = 13,
				TextColor3 = RiftUI.Theme.Text,
				BackgroundTransparency = 1,
				Size = UDim2.new(1, 0, 0, 22),
				TextXAlignment = Enum.TextXAlignment.Left,
				Parent = dropdownFrame
			})
			
			local dropdownBox = Create("TextButton", {
				Name = "DropdownBox",
				Text = default or "Select...",
				Font = Enum.Font.Gotham,
				TextSize = 12,
				TextColor3 = RiftUI.Theme.TextSecondary,
				BackgroundColor3 = RiftUI.Theme.BackgroundSecondary,
				Size = UDim2.new(1, 0, 0, 34),
				Position = UDim2.new(0, 0, 0, 24),
				TextXAlignment = Enum.TextXAlignment.Left,
				Parent = dropdownFrame
			})
			SetPadding(dropdownBox, 12)
			SetCorner(dropdownBox, UDim.new(0, 8))
			
			local arrow = Create("ImageLabel", {
				Name = "Arrow",
				Size = UDim2.new(0, 14, 0, 14),
				Position = UDim2.new(1, -28, 0.5, -7),
				BackgroundTransparency = 1,
				Image = "rbxassetid://3926307971",
				ImageRectOffset = Vector2.new(324, 524),
				ImageRectSize = Vector2.new(36, 36),
				ImageColor3 = RiftUI.Theme.TextSecondary,
				Rotation = 0,
				Parent = dropdownBox
			})
			
			local listFrame = Create("Frame", {
				Name = "ListFrame",
				Size = UDim2.new(1, 0, 0, 0),
				Position = UDim2.new(0, 0, 0, 62),
				BackgroundColor3 = RiftUI.Theme.BackgroundSecondary,
				BorderSizePixel = 0,
				ClipsDescendants = true,
				Visible = false,
				ZIndex = 50,
				Parent = dropdownFrame
			})
			SetCorner(listFrame, UDim.new(0, 8))
			SetStroke(listFrame, RiftUI.Theme.Stroke, 1)
			
			local searchBox = Create("TextBox", {
				Name = "SearchBox",
				PlaceholderText = "Search...",
				Text = "",
				Font = Enum.Font.Gotham,
				TextSize = 12,
				TextColor3 = RiftUI.Theme.Text,
				PlaceholderColor3 = RiftUI.Theme.TextDark,
				BackgroundColor3 = RiftUI.Theme.Background,
				Size = UDim2.new(1, -20, 0, 30),
				Position = UDim2.new(0, 10, 0, 10),
				ZIndex = 51,
				Parent = listFrame
			})
			SetCorner(searchBox, UDim.new(0, 6))
			SetPadding(searchBox, 10)
			
			local optionsScroll = Create("ScrollingFrame", {
				Name = "OptionsScroll",
				Size = UDim2.new(1, -20, 1, -50),
				Position = UDim2.new(0, 10, 0, 46),
				BackgroundTransparency = 1,
				ScrollBarThickness = 3,
				ScrollBarImageColor3 = RiftUI.Theme.BackgroundTertiary,
				CanvasSize = UDim2.new(0, 0, 0, 0),
				ZIndex = 51,
				Parent = listFrame
			})
			
			local optionsList = Create("UIListLayout", {
				Padding = UDim.new(0, 4),
				Parent = optionsScroll
			})
			
			local optionButtons = {}
			local expanded = false
			
			local function UpdateOptions(filter)
				for _, btn in ipairs(optionButtons) do
					btn:Destroy()
				end
				optionButtons = {}
				
				for _, option in ipairs(options) do
					if not filter or string.find(string.lower(option), string.lower(filter), 1, true) then
						local optionBtn = Create("TextButton", {
							Name = option,
							Text = option,
							Font = Enum.Font.Gotham,
							TextSize = 12,
							TextColor3 = RiftUI.Theme.TextSecondary,
							BackgroundColor3 = RiftUI.Theme.Background,
							Size = UDim2.new(1, 0, 0, 30),
							ZIndex = 51,
							Parent = optionsScroll
						})
						SetCorner(optionBtn, UDim.new(0, 6))
						
						optionBtn.MouseEnter:Connect(function()
							Tween(optionBtn, RiftUI.Animation.Fast, {BackgroundColor3 = RiftUI.Theme.Hover, TextColor3 = RiftUI.Theme.Text})
						end)
						
						optionBtn.MouseLeave:Connect(function()
							Tween(optionBtn, RiftUI.Animation.Fast, {BackgroundColor3 = RiftUI.Theme.Background, TextColor3 = RiftUI.Theme.TextSecondary})
						end)
						
						optionBtn.MouseButton1Click:Connect(function()
							dropdownBox.Text = option
							callback(option)
							expanded = false
							Tween(listFrame, RiftUI.Animation.Smooth, {Size = UDim2.new(1, 0, 0, 0)})
							Tween(arrow, RiftUI.Animation.Fast, {Rotation = 0})
							task.delay(0.3, function()
								listFrame.Visible = false
								dropdownFrame.Size = UDim2.new(1, 0, 0, 38)
							end)
						end)
						
						table.insert(optionButtons, optionBtn)
					end
				end
				
				optionsScroll.CanvasSize = UDim2.new(0, 0, 0, math.min(#optionButtons * 34, 160))
			end
			
			UpdateOptions()
			
			searchBox:GetPropertyChangedSignal("Text"):Connect(function()
				UpdateOptions(searchBox.Text)
			end)
			
			dropdownBox.MouseButton1Click:Connect(function()
				expanded = not expanded
				if expanded then
					listFrame.Visible = true
					dropdownFrame.Size = UDim2.new(1, 0, 0, 220)
					Tween(listFrame, RiftUI.Animation.Smooth, {Size = UDim2.new(1, 0, 0, 160)})
					Tween(arrow, RiftUI.Animation.Fast, {Rotation = 180})
				else
					Tween(listFrame, RiftUI.Animation.Smooth, {Size = UDim2.new(1, 0, 0, 0)})
					Tween(arrow, RiftUI.Animation.Fast, {Rotation = 0})
					task.delay(0.3, function()
						listFrame.Visible = false
						dropdownFrame.Size = UDim2.new(1, 0, 0, 38)
					end)
				end
			end)
			
			dropdownBox.MouseEnter:Connect(function()
				Tween(dropdownBox, RiftUI.Animation.Fast, {BackgroundColor3 = RiftUI.Theme.Hover})
			end)
			
			dropdownBox.MouseLeave:Connect(function()
				Tween(dropdownBox, RiftUI.Animation.Fast, {BackgroundColor3 = RiftUI.Theme.BackgroundSecondary})
			end)
			
			return {
				Set = function(value)
					dropdownBox.Text = value
					callback(value)
				end,
				Get = function()
					return dropdownBox.Text
				end,
				Refresh = function(newOptions)
					options = newOptions
					UpdateOptions()
				end
			}
		end
		
		function tab:AddInput(config)
			config = config or {}
			local text = config.text or "Input"
			local placeholder = config.placeholder or "Enter text..."
			local default = config.default or ""
			local callback = config.callback or function() end
			
			local inputFrame = Create("Frame", {
				Name = "Input",
				Size = UDim2.new(1, 0, 0, 64),
				BackgroundTransparency = 1,
				Parent = self.Page
			})
			
			local label = Create("TextLabel", {
				Name = "Label",
				Text = text,
				Font = Enum.Font.Gotham,
				TextSize = 13,
				TextColor3 = RiftUI.Theme.Text,
				BackgroundTransparency = 1,
				Size = UDim2.new(1, 0, 0, 22),
				TextXAlignment = Enum.TextXAlignment.Left,
				Parent = inputFrame
			})
			
			local textBox = Create("TextBox", {
				Name = "TextBox",
				PlaceholderText = placeholder,
				Text = default,
				Font = Enum.Font.Code,
				TextSize = 12,
				TextColor3 = RiftUI.Theme.Text,
				PlaceholderColor3 = RiftUI.Theme.TextDark,
				BackgroundColor3 = RiftUI.Theme.BackgroundSecondary,
				Size = UDim2.new(1, 0, 0, 36),
				Position = UDim2.new(0, 0, 0, 26),
				TextXAlignment = Enum.TextXAlignment.Left,
				ClearTextOnFocus = false,
				Parent = inputFrame
			})
			SetPadding(textBox, 12)
			SetCorner(textBox, UDim.new(0, 8))
			
			local cursor = Create("Frame", {
				Name = "Cursor",
				Size = UDim2.new(0, 2, 0, 18),
				Position = UDim2.new(0, 14 + TextService:GetTextSize(default, 12, Enum.Font.Code, Vector2.new(9999, 9999)).X, 0, 9),
				BackgroundColor3 = accentColor,
				BorderSizePixel = 0,
				Visible = false,
				Parent = textBox
			})
			
			local cursorTween = TweenService:Create(cursor, TweenInfo.new(0.5, Enum.EasingStyle.Linear, Enum.EasingDirection.InOut, -1, true), {BackgroundTransparency = 1})
			
			textBox.Focused:Connect(function()
				Tween(textBox, RiftUI.Animation.Fast, {BackgroundColor3 = RiftUI.Theme.BackgroundTertiary})
				cursor.Visible = true
				cursorTween:Play()
			end)
			
			textBox.FocusLost:Connect(function()
				Tween(textBox, RiftUI.Animation.Fast, {BackgroundColor3 = RiftUI.Theme.BackgroundSecondary})
				cursor.Visible = false
				cursorTween:Cancel()
				callback(textBox.Text)
			end)
			
			textBox:GetPropertyChangedSignal("Text"):Connect(function()
				local textSize = TextService:GetTextSize(textBox.Text, 12, Enum.Font.Code, Vector2.new(9999, 9999))
				cursor.Position = UDim2.new(0, 14 + textSize.X, 0, 9)
			end)
			
			return {
				Set = function(value)
					textBox.Text = value
					callback(value)
				end,
				Get = function()
					return textBox.Text
				end
			}
		end
		
		function tab:AddKeybind(config)
			config = config or {}
			local text = config.text or "Keybind"
			local default = config.default or Enum.KeyCode.Unknown
			local callback = config.callback or function() end
			
			local keybindFrame = Create("Frame", {
				Name = "Keybind",
				Size = UDim2.new(1, 0, 0, 40),
				BackgroundTransparency = 1,
				Parent = self.Page
			})
			
			local label = Create("TextLabel", {
				Name = "Label",
				Text = text,
				Font = Enum.Font.Gotham,
				TextSize = 13,
				TextColor3 = RiftUI.Theme.Text,
				BackgroundTransparency = 1,
				Size = UDim2.new(1, -90, 1, 0),
				TextXAlignment = Enum.TextXAlignment.Left,
				Parent = keybindFrame
			})
			
			local keyBox = Create("TextButton", {
				Name = "KeyBox",
				Text = default ~= Enum.KeyCode.Unknown and default.Name or "None",
				Font = Enum.Font.GothamBold,
				TextSize = 11,
				TextColor3 = RiftUI.Theme.Text,
				BackgroundColor3 = RiftUI.Theme.BackgroundSecondary,
				Size = UDim2.new(0, 80, 0, 32),
				Position = UDim2.new(1, -80, 0.5, -16),
				Parent = keybindFrame
			})
			SetCorner(keyBox, UDim.new(0, 8))
			
			local keyGlow = Create("Frame", {
				Name = "KeyGlow",
				Size = UDim2.new(1, 6, 1, 6),
				Position = UDim2.new(0, -3, 0, -3),
				BackgroundColor3 = accentColor,
				BackgroundTransparency = 1,
				BorderSizePixel = 0,
				ZIndex = keyBox.ZIndex - 1,
				Parent = keyBox
			})
			SetCorner(keyGlow, UDim.new(0, 10))
			
			local currentKey = default
			local listening = false
			
			keyBox.MouseButton1Click:Connect(function()
				if listening then return end
				listening = true
				keyBox.Text = "..."
				Tween(keyBox, RiftUI.Animation.Fast, {BackgroundColor3 = accentColor, TextColor3 = RiftUI.Theme.Background})
				Tween(keyGlow, RiftUI.Animation.Fast, {BackgroundTransparency = 0.7})
				
				local connection
				connection = UserInputService.InputBegan:Connect(function(input)
					if input.UserInputType == Enum.UserInputType.Keyboard then
						if input.KeyCode ~= Enum.KeyCode.Escape then
							currentKey = input.KeyCode
							keyBox.Text = input.KeyCode.Name
							callback(input.KeyCode)
						else
							keyBox.Text = currentKey ~= Enum.KeyCode.Unknown and currentKey.Name or "None"
						end
						listening = false
						Tween(keyBox, RiftUI.Animation.Fast, {BackgroundColor3 = RiftUI.Theme.BackgroundSecondary, TextColor3 = RiftUI.Theme.Text})
						Tween(keyGlow, RiftUI.Animation.Fast, {BackgroundTransparency = 1})
						connection:Disconnect()
					end
				end)
			end)
			
			keyBox.MouseEnter:Connect(function()
				if not listening then
					Tween(keyBox, RiftUI.Animation.Fast, {BackgroundColor3 = RiftUI.Theme.Hover})
					Tween(keyGlow, RiftUI.Animation.Fast, {BackgroundTransparency = 0.85})
				end
			end)
			
			keyBox.MouseLeave:Connect(function()
				if not listening then
					Tween(keyBox, RiftUI.Animation.Fast, {BackgroundColor3 = RiftUI.Theme.BackgroundSecondary})
					Tween(keyGlow, RiftUI.Animation.Fast, {BackgroundTransparency = 1})
				end
			end)
			
			return {
				Set = function(keyCode)
					currentKey = keyCode
					keyBox.Text = keyCode ~= Enum.KeyCode.Unknown and keyCode.Name or "None"
					callback(keyCode)
				end,
				Get = function()
					return currentKey
				end
			}
		end
		
		function tab:AddColorPicker(config)
			config = config or {}
			local text = config.text or "Color Picker"
			local default = config.default or Color3.fromRGB(255, 255, 255)
			local callback = config.callback or function() end
			
			local colorFrame = Create("Frame", {
				Name = "ColorPicker",
				Size = UDim2.new(1, 0, 0, 44),
				BackgroundTransparency = 1,
				Parent = self.Page
			})
			
			local label = Create("TextLabel", {
				Name = "Label",
				Text = text,
				Font = Enum.Font.Gotham,
				TextSize = 13,
				TextColor3 = RiftUI.Theme.Text,
				BackgroundTransparency = 1,
				Size = UDim2.new(1, -56, 1, 0),
				TextXAlignment = Enum.TextXAlignment.Left,
				Parent = colorFrame
			})
			
			local preview = Create("TextButton", {
				Name = "Preview",
				Text = "",
				BackgroundColor3 = default,
				Size = UDim2.new(0, 40, 0, 32),
				Position = UDim2.new(1, -40, 0.5, -16),
				Parent = colorFrame
			})
			SetCorner(preview, UDim.new(0, 8))
			SetStroke(preview, RiftUI.Theme.Stroke, 1)
			
			local pickerFrame = Create("Frame", {
				Name = "PickerFrame",
				Size = UDim2.new(1, 0, 0, 0),
				Position = UDim2.new(0, 0, 0, 48),
				BackgroundColor3 = RiftUI.Theme.BackgroundSecondary,
				BorderSizePixel = 0,
				ClipsDescendants = true,
				Visible = false,
				Parent = colorFrame
			})
			SetCorner(pickerFrame, UDim.new(0, 10))
			SetStroke(pickerFrame, RiftUI.Theme.Stroke, 1)
			
			local saturationFrame = Create("Frame", {
				Name = "Saturation",
				Size = UDim2.new(1, -24, 0, 110),
				Position = UDim2.new(0, 12, 0, 12),
				BackgroundColor3 = Color3.fromRGB(255, 0, 0),
				BorderSizePixel = 0,
				Parent = pickerFrame
			})
			SetCorner(saturationFrame, UDim.new(0, 8))
			
			local saturationGradient = Create("UIGradient", {
				Color = ColorSequence.new({
					ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 255, 255)),
					ColorSequenceKeypoint.new(1, Color3.fromRGB(255, 0, 0))
				}),
				Parent = saturationFrame
			})
			
			local darknessFrame = Create("Frame", {
				Name = "Darkness",
				Size = UDim2.new(1, 0, 1, 0),
				BackgroundTransparency = 0,
				BorderSizePixel = 0,
				Parent = saturationFrame
			})
			SetCorner(darknessFrame, UDim.new(0, 8))
			
			local darknessGradient = Create("UIGradient", {
				Rotation = 90,
				Color = ColorSequence.new({
					ColorSequenceKeypoint.new(0, Color3.fromRGB(0, 0, 0)),
					ColorSequenceKeypoint.new(1, Color3.fromRGB(0, 0, 0))
				}),
				Transparency = NumberSequence.new({
					NumberSequenceKeypoint.new(0, 0),
					NumberSequenceKeypoint.new(1, 1)
				}),
				Parent = darknessFrame
			})
			
			local selector = Create("Frame", {
				Name = "Selector",
				Size = UDim2.new(0, 14, 0, 14),
				BackgroundColor3 = RiftUI.Theme.Text,
				BorderSizePixel = 0,
				Position = UDim2.new(0.5, -7, 0.5, -7),
				Parent = saturationFrame
			})
			SetCorner(selector, UDim.new(1, 0))
			SetStroke(selector, Color3.fromRGB(0, 0, 0), 2)
			
			local hueSlider = Create("Frame", {
				Name = "HueSlider",
				Size = UDim2.new(1, -24, 0, 18),
				Position = UDim2.new(0, 12, 0, 130),
				BackgroundColor3 = Color3.fromRGB(255, 255, 255),
				BorderSizePixel = 0,
				Parent = pickerFrame
			})
			SetCorner(hueSlider, UDim.new(0, 9))
			
			local hueGradient = Create("UIGradient", {
				Color = ColorSequence.new({
					ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 0, 0)),
					ColorSequenceKeypoint.new(0.167, Color3.fromRGB(255, 255, 0)),
					ColorSequenceKeypoint.new(0.333, Color3.fromRGB(0, 255, 0)),
					ColorSequenceKeypoint.new(0.5, Color3.fromRGB(0, 255, 255)),
					ColorSequenceKeypoint.new(0.667, Color3.fromRGB(0, 0, 255)),
					ColorSequenceKeypoint.new(0.833, Color3.fromRGB(255, 0, 255)),
					ColorSequenceKeypoint.new(1, Color3.fromRGB(255, 0, 0))
				}),
				Parent = hueSlider
			})
			
			local hueHandle = Create("Frame", {
				Name = "HueHandle",
				Size = UDim2.new(0, 10, 1, 6),
				Position = UDim2.new(0, -5, 0, -3),
				BackgroundColor3 = RiftUI.Theme.Text,
				BorderSizePixel = 0,
				Parent = hueSlider
			})
			SetCorner(hueHandle, UDim.new(0, 5))
			SetStroke(hueHandle, Color3.fromRGB(0, 0, 0), 1)
			
			local expanded = false
			local hue = 0
			local saturation = 1
			local value = 1
			
			local function UpdateColor()
				local color = Color3.fromHSV(hue, saturation, value)
				preview.BackgroundColor3 = color
				saturationFrame.BackgroundColor3 = Color3.fromHSV(hue, 1, 1)
				callback(color)
			end
			
			local function HandleSaturationInput(input)
				local pos = Vector2.new(
					math.clamp((input.Position.X - saturationFrame.AbsolutePosition.X) / saturationFrame.AbsoluteSize.X, 0, 1),
					math.clamp((input.Position.Y - saturationFrame.AbsolutePosition.Y) / saturationFrame.AbsoluteSize.Y, 0, 1)
				)
				saturation = pos.X
				value = 1 - pos.Y
				Tween(selector, RiftUI.Animation.Fast, {Position = UDim2.new(pos.X, -7, pos.Y, -7)})
				UpdateColor()
			end
			
			local function HandleHueInput(input)
				local pos = math.clamp((input.Position.X - hueSlider.AbsolutePosition.X) / hueSlider.AbsoluteSize.X, 0, 1)
				hue = pos
				Tween(hueHandle, RiftUI.Animation.Fast, {Position = UDim2.new(pos, -5, 0, -3)})
				UpdateColor()
			end
			
			local draggingSat = false
			local draggingHue = false
			
			saturationFrame.InputBegan:Connect(function(input)
				if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
					draggingSat = true
					HandleSaturationInput(input)
				end
			end)
			
			saturationFrame.InputChanged:Connect(function(input)
				if draggingSat and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
					HandleSaturationInput(input)
				end
			end)
			
			hueSlider.InputBegan:Connect(function(input)
				if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
					draggingHue = true
					HandleHueInput(input)
				end
			end)
			
			hueSlider.InputChanged:Connect(function(input)
				if draggingHue and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
					HandleHueInput(input)
				end
			end)
			
			UserInputService.InputEnded:Connect(function(input)
				if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
					draggingSat = false
					draggingHue = false
				end
			end)
			
			preview.MouseButton1Click:Connect(function()
				expanded = not expanded
				if expanded then
					pickerFrame.Visible = true
					colorFrame.Size = UDim2.new(1, 0, 0, 210)
					Tween(pickerFrame, RiftUI.Animation.Smooth, {Size = UDim2.new(1, 0, 0, 165)})
				else
					Tween(pickerFrame, RiftUI.Animation.Smooth, {Size = UDim2.new(1, 0, 0, 0)})
					task.delay(0.3, function()
						pickerFrame.Visible = false
						colorFrame.Size = UDim2.new(1, 0, 0, 44)
					end)
				end
			end)
			
			return {
				Set = function(color)
					local h, s, v = color:ToHSV()
					hue, saturation, value = h, s, v
					preview.BackgroundColor3 = color
					saturationFrame.BackgroundColor3 = Color3.fromHSV(h, 1, 1)
					Tween(selector, RiftUI.Animation.Fast, {Position = UDim2.new(s, -7, 1 - v, -7)})
					Tween(hueHandle, RiftUI.Animation.Fast, {Position = UDim2.new(h, -5, 0, -3)})
					callback(color)
				end,
				Get = function()
					return preview.BackgroundColor3
				end
			}
		end
		
		function tab:AddImage(config)
			config = config or {}
			local imageId = config.image or ""
			local size = config.size or UDim2.new(1, 0, 0, 160)
			local callback = config.callback or function() end
			
			local imageFrame = Create("Frame", {
				Name = "Image",
				Size = size,
				BackgroundColor3 = RiftUI.Theme.BackgroundSecondary,
				BorderSizePixel = 0,
				Parent = self.Page
			})
			SetCorner(imageFrame, UDim.new(0, 10))
			
			local imageLabel = Create("ImageLabel", {
				Name = "ImageLabel",
				Size = UDim2.new(1, -10, 1, -10),
				Position = UDim2.new(0, 5, 0, 5),
				BackgroundTransparency = 1,
				Image = imageId,
				ScaleType = Enum.ScaleType.Fit,
				Parent = imageFrame
			})
			SetCorner(imageLabel, UDim.new(0, 8))
			
			local clickOverlay = Create("Frame", {
				Name = "ClickOverlay",
				Size = UDim2.new(1, 0, 1, 0),
				BackgroundColor3 = accentColor,
				BackgroundTransparency = 1,
				BorderSizePixel = 0,
				Parent = imageFrame
			})
			SetCorner(clickOverlay, UDim.new(0, 10))
			
			imageFrame.InputBegan:Connect(function(input)
				if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
					Tween(imageFrame, RiftUI.Animation.Fast, {Size = UDim2.new(size.X.Scale, size.X.Offset - 4, size.Y.Scale, size.Y.Offset - 4), Position = UDim2.new(0, 2, 0, 2)})
					Tween(clickOverlay, RiftUI.Animation.Fast, {BackgroundTransparency = 0.8})
				end
			end)
			
			imageFrame.InputEnded:Connect(function(input)
				if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
					Tween(imageFrame, RiftUI.Animation.Elastic, {Size = size, Position = UDim2.new(0, 0, 0, 0)})
					Tween(clickOverlay, RiftUI.Animation.Fast, {BackgroundTransparency = 1})
					callback()
				end
			end)
			
			return {
				Set = function(newImageId)
					imageLabel.Image = newImageId
				end,
				Get = function()
					return imageLabel.Image
				end
			}
		end
		
		table.insert(self.Tabs, tab)
		self.TabScroll.CanvasSize = UDim2.new(0, 0, 0, #self.Tabs * 38 + 28)
		
		return tab
	end
	
	function window:SelectTab(tab)
		if self.CurrentTab == tab then return end
		
		if self.CurrentTab then
			Tween(self.CurrentTab.Button, RiftUI.Animation.Fast, {TextColor3 = RiftUI.Theme.TextSecondary, BackgroundColor3 = RiftUI.Theme.Background})
			self.CurrentTab.Page.Visible = false
		end
		
		self.CurrentTab = tab
		Tween(tab.Button, RiftUI.Animation.Fast, {TextColor3 = RiftUI.Theme.Text, BackgroundColor3 = RiftUI.Theme.BackgroundSecondary})
		
		local indicatorY = tab.Button.LayoutOrder * 38 + 14
		for i, t in ipairs(self.Tabs) do
			if t == tab then
				indicatorY = (i - 1) * 38 + 14
				break
			end
		end
		
		Tween(self.Indicator, RiftUI.Animation.Smooth, {Position = UDim2.new(0, 0, 0, indicatorY)})
		
		tab.Page.Visible = true
		tab.Page.Position = UDim2.new(0, 15, 0, 0)
		tab.Page.BackgroundTransparency = 1
		Tween(tab.Page, RiftUI.Animation.Smooth, {Position = UDim2.new(0, 0, 0, 0)})
	end
	
	function window:Notify(config)
		config = config or {}
		local title = config.title or "Notification"
		local text = config.text or ""
		local icon = config.icon or ""
		local duration = config.duration or 3
		local position = config.position or "top-right"
		
		local notificationGui = Create("ScreenGui", {
			Name = "RiftNotification",
			Parent = PlayerGui,
			ResetOnSpawn = false,
			ZIndexBehavior = Enum.ZIndexBehavior.Sibling
		})
		
		local notificationFrame = Create("Frame", {
			Name = "Notification",
			Size = UDim2.new(0, 300, 0, 86),
			BackgroundColor3 = RiftUI.Theme.Background,
			BorderSizePixel = 0,
			Position = position == "top-right" and UDim2.new(1, 320, 0, 20) or
			           position == "top-left" and UDim2.new(0, -320, 0, 20) or
			           position == "bottom-right" and UDim2.new(1, 320, 1, -106) or
			           UDim2.new(0, -320, 1, -106),
			Parent = notificationGui
		})
		SetCorner(notificationFrame, UDim.new(0, 10))
		SetStroke(notificationFrame, RiftUI.Theme.Stroke, 1)
		SetShadow(notificationFrame, 0.7)
		
		local iconLabel = Create("ImageLabel", {
			Name = "Icon",
			Size = UDim2.new(0, 42, 0, 42),
			Position = UDim2.new(0, 16, 0, 22),
			BackgroundTransparency = 1,
			Image = icon,
			Parent = notificationFrame
		})
		SetCorner(iconLabel, UDim.new(0, 8))
		
		local titleLabel = Create("TextLabel", {
			Name = "Title",
			Text = title,
			Font = Enum.Font.GothamBold,
			TextSize = 14,
			TextColor3 = RiftUI.Theme.Text,
			BackgroundTransparency = 1,
			Size = UDim2.new(1, -80, 0, 20),
			Position = UDim2.new(0, 70, 0, 16),
			TextXAlignment = Enum.TextXAlignment.Left,
			Parent = notificationFrame
		})
		
		local textLabel = Create("TextLabel", {
			Name = "Text",
			Text = text,
			Font = Enum.Font.Gotham,
			TextSize = 12,
			TextColor3 = RiftUI.Theme.TextSecondary,
			BackgroundTransparency = 1,
			Size = UDim2.new(1, -80, 0, 44),
			Position = UDim2.new(0, 70, 0, 36),
			TextWrapped = true,
			TextXAlignment = Enum.TextXAlignment.Left,
			TextYAlignment = Enum.TextYAlignment.Top,
			Parent = notificationFrame
		})
		
		local closeBtn = Create("TextButton", {
			Name = "Close",
			Text = "X",
			Font = Enum.Font.GothamBold,
			TextSize = 12,
			TextColor3 = RiftUI.Theme.TextSecondary,
			BackgroundTransparency = 1,
			Size = UDim2.new(0, 26, 0, 26),
			Position = UDim2.new(1, -30, 0, 10),
			Parent = notificationFrame
		})
		
		closeBtn.MouseEnter:Connect(function()
			Tween(closeBtn, RiftUI.Animation.Fast, {TextColor3 = RiftUI.Theme.Error})
		end)
		
		closeBtn.MouseLeave:Connect(function()
			Tween(closeBtn, RiftUI.Animation.Fast, {TextColor3 = RiftUI.Theme.TextSecondary})
		end)
		
		local function CloseNotification()
			Tween(notificationFrame, RiftUI.Animation.Smooth, {Position = position:find("right") and UDim2.new(1, 320, notificationFrame.Position.Y.Scale, notificationFrame.Position.Y.Offset) or UDim2.new(0, -320, notificationFrame.Position.Y.Scale, notificationFrame.Position.Y.Offset)})
			task.delay(0.35, function()
				notificationGui:Destroy()
			end)
		end
		
		closeBtn.MouseButton1Click:Connect(CloseNotification)
		
		Tween(notificationFrame, RiftUI.Animation.Elastic, {Position = position:find("right") and UDim2.new(1, -320, notificationFrame.Position.Y.Scale, notificationFrame.Position.Y.Offset) or UDim2.new(0, 20, notificationFrame.Position.Y.Scale, notificationFrame.Position.Y.Offset)})
		
		if duration > 0 then
			task.delay(duration, CloseNotification)
		end
	end
	
	Tween(mainFrame, RiftUI.Animation.Elastic, {Size = UDim2.new(0, size.X, 0, size.Y), Position = UDim2.new(0.5, -size.X/2, 0.5, -size.Y/2)})
	
	return window
end

return RiftUI
